# reusable date picker 

We are building a reusable date picker this allows us to picker flutter dates and romats them in the day month year format
this widget is good for picking things like date for date of birth that are resultant in major applications
